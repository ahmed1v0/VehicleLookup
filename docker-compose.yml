services:
  api:
    build:
      context: ./backend
      dockerfile: VehicleLookup.Api/Dockerfile
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend/car-app
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    depends_on:
      - api
